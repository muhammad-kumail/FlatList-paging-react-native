# Flatlist-paging-react-native
Fetching users data from sample API. it's a server side paging. 6 users per page (request). Implemented in FlatList.
## Demo

## Installing & Execution
Run commands: `npm install` `npm run android`